# Changesets

This directory contains auto-generated files by [Changesets](https://github.com/changesets/changesets).

To create a changeset, run:

```bash
npm run changeset
```

Then select the packages and the type of version bump (major/minor/patch).
Commit the generated markdown file to trigger a version bump on CI.
